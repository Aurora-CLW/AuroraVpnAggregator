# 模块接口设计文档

## 1. 模块总览

```
src/
├── core/                    # 核心模块
│   ├── fetcher.py          # 订阅抓取
│   ├── parser.py           # 节点解析
│   ├── tester.py           # 节点测试
│   ├── generator.py        # 订阅生成
│   └── deduplicator.py     # 节点去重
│
├── handlers/                # 订阅源处理器
│   ├── base.py             # 基类
│   ├── github_handler.py   # GitHub 处理器
│   ├── local_handler.py    # 本地文件处理器
│   └── telegram_handler.py # Telegram 处理器
│
├── models/                  # 数据模型
│   └── node.py             # 节点模型
│
└── utils/                   # 工具函数
    ├── logger.py           # 日志
    ├── network.py          # 网络工具
    └── geoip.py            # 地理识别
```

---

## 2. 核心模块接口

### 2.1 Fetcher - 订阅抓取器

```python
class Fetcher:
    """订阅源抓取器"""

    def __init__(self, timeout: int = 30, retry: int = 3):
        """
        初始化抓取器

        Args:
            timeout: 请求超时时间（秒）
            retry: 失败重试次数
        """

    async def fetch_url(self, url: str, source_name: str = "") -> Optional[str]:
        """
        抓取远程订阅内容

        Args:
            url: 订阅地址
            source_name: 来源名称（用于日志）

        Returns:
            订阅内容字符串，失败返回 None
        """

    def fetch_file(self, path: str, source_name: str = "") -> Optional[str]:
        """
        读取本地文件内容

        Args:
            path: 文件路径
            source_name: 来源名称

        Returns:
            文件内容字符串，失败返回 None
        """

    async def fetch_source(self, source: Source, parse: bool = True) -> List[Node]:
        """
        抓取并解析订阅源

        Args:
            source: 订阅源配置
            parse: 是否解析节点

        Returns:
            节点列表
        """

    async def fetch_all(self, sources: List[Source], concurrent: int = 5) -> List[Node]:
        """
        并发抓取多个订阅源

        Args:
            sources: 订阅源列表
            concurrent: 并发数

        Returns:
            所有节点列表
        """
```

### 2.2 Parser - 节点解析器

```python
class Parser:
    """节点解析器"""

    def parse(self, content: str, format: str = "auto") -> List[Node]:
        """
        自动识别并解析内容

        Args:
            content: 订阅内容
            format: 格式 (auto, clash, base64, singbox, urls)

        Returns:
            节点列表
        """

    def _detect_format(self, content: str) -> str:
        """自动检测内容格式"""

    def _parse_clash(self, content: str) -> List[Node]:
        """解析 Clash YAML 格式"""

    def _parse_base64(self, content: str) -> List[Node]:
        """解析 Base64 编码内容"""

    def _parse_urls(self, content: str) -> List[Node]:
        """解析 URL 列表"""

    def _parse_singbox(self, content: str) -> List[Node]:
        """解析 Sing-box JSON 格式"""

    def _parse_node_url(self, url: str) -> Optional[Node]:
        """解析单个节点 URL"""
```

### 2.3 Tester - 节点测试器

```python
class NodeTester:
    """节点测试器"""

    def __init__(self, config: dict):
        """
        初始化测试器

        Args:
            config: 测试配置
                - tcp_timeout: TCP 超时（秒）
                - http_timeout: HTTP 超时（秒）
                - tcp_concurrent: TCP 并发数
                - http_concurrent: HTTP 并发数
                - test_url: 测试 URL
        """

    async def test_all(self, nodes: List[Node]) -> List[Node]:
        """
        测试所有节点

        Args:
            nodes: 待测试节点列表

        Returns:
            有效节点列表
        """

    async def _tcp_batch_test(self, nodes: List[Node]) -> List[Node]:
        """批量 TCP 测试"""

    def _tcp_test(self, node: Node) -> bool:
        """单个 TCP 测试"""

    async def _http_batch_test(self, nodes: List[Node]) -> List[Node]:
        """批量代理 HTTP 测试"""

    async def _http_test(self, node: Node) -> bool:
        """单个代理 HTTP 测试"""
```

### 2.4 Generator - 订阅生成器

```python
class Generator:
    """订阅生成器"""

    def __init__(self, config: dict):
        """
        初始化生成器

        Args:
            config: 生成配置
                - naming: 节点命名格式
                - sort_by: 排序方式
                - max_nodes: 最大节点数
        """

    def generate_all(self, nodes: List[Node], output_dir: str):
        """
        生成所有格式的订阅

        Args:
            nodes: 节点列表
            output_dir: 输出目录
        """

    def generate_clash(self, nodes: List[Node]) -> str:
        """
        生成 Clash YAML

        Args:
            nodes: 节点列表

        Returns:
            YAML 字符串
        """

    def generate_v2ray(self, nodes: List[Node]) -> str:
        """
        生成 V2Ray Base64

        Args:
            nodes: 节点列表

        Returns:
            Base64 字符串
        """

    def generate_singbox(self, nodes: List[Node]) -> str:
        """
        生成 Sing-box JSON

        Args:
            nodes: 节点列表

        Returns:
            JSON 字符串
        """
```

### 2.5 Deduplicator - 节点去重器

```python
class Deduplicator:
    """节点去重器"""

    def __init__(self, method: str = "fingerprint"):
        """
        初始化去重器

        Args:
            method: 去重方式 (fingerprint/server_port)
        """

    def deduplicate(self, nodes: List[Node]) -> List[Node]:
        """
        去重

        Args:
            nodes: 节点列表

        Returns:
            去重后的节点列表
        """

    def _generate_fingerprint(self, node: Node) -> str:
        """生成节点指纹"""
```

---

## 3. 处理器模块接口

### 3.1 BaseHandler - 处理器基类

```python
from abc import ABC, abstractmethod

class BaseHandler(ABC):
    """订阅源处理器基类"""

    def __init__(self, config: dict):
        """
        初始化处理器

        Args:
            config: 处理器配置
        """
        self.config = config
        self.enabled = config.get("enabled", True)

    @abstractmethod
    async def fetch(self) -> List[Node]:
        """
        抓取节点

        Returns:
            节点列表
        """

    @abstractmethod
    def parse(self, content: str) -> List[Node]:
        """
        解析节点

        Args:
            content: 订阅内容

        Returns:
            节点列表
        """

    def mark_source(self, nodes: List[Node], source_name: str) -> List[Node]:
        """
        标记节点来源

        Args:
            nodes: 节点列表
            source_name: 来源名称

        Returns:
            标记后的节点列表
        """
```

### 3.2 GitHubHandler - GitHub 处理器

```python
class GitHubHandler(BaseHandler):
    """GitHub 订阅处理器"""

    async def fetch(self) -> List[Node]:
        """抓取 GitHub 订阅"""

    def parse(self, content: str) -> List[Node]:
        """解析订阅内容"""
```

### 3.3 LocalHandler - 本地文件处理器

```python
class LocalHandler(BaseHandler):
    """本地文件处理器"""

    async def fetch(self) -> List[Node]:
        """读取本地文件"""

    def parse(self, content: str) -> List[Node]:
        """解析文件内容"""

    def watch_file(self, callback: Callable):
        """
        监控文件变更

        Args:
            callback: 变更回调函数
        """
```

### 3.4 TelegramHandler - Telegram 处理器

```python
class TelegramHandler(BaseHandler):
    """Telegram 频道处理器"""

    NODE_PATTERNS = [
        r'(vmess://[A-Za-z0-9+/=]+)',
        r'(vless://[A-Za-z0-9\-._~:/?#\[\]@!$&\'()*+,;=%]+)',
        # ...
    ]

    async def init_client(self):
        """初始化 Telegram 客户端"""

    async def fetch(self) -> List[Node]:
        """从 Telegram 频道抓取节点"""

    async def _fetch_channel(self, channel_config: dict) -> List[Node]:
        """抓取单个频道"""

    def _extract_nodes(self, text: str, keywords: List[str] = None) -> List[Node]:
        """从文本中提取节点"""

    async def close(self):
        """关闭客户端连接"""
```

---

## 4. 数据模型接口

### 4.1 Node - 节点模型

```python
@dataclass
class Node:
    """VPN 节点数据模型"""

    # 基础信息
    name: str
    type: str
    server: str
    port: int

    # ... 其他字段

    def generate_fingerprint(self) -> str:
        """生成节点指纹，用于去重"""

    def to_clash(self) -> dict:
        """转换为 Clash 代理格式"""

    def to_v2ray_url(self) -> str:
        """转换为 V2Ray URL 格式"""

    def to_singbox(self) -> dict:
        """转换为 Sing-box outbound 格式"""
```

### 4.2 Source - 订阅源模型

```python
@dataclass
class Source:
    """订阅源数据模型"""

    name: str
    type: str
    enabled: bool = True
    priority: int = 5
    url: Optional[str] = None
    path: Optional[str] = None
    format: str = "auto"
    last_update: Optional[datetime] = None
    node_count: int = 0
    error: Optional[str] = None
```

### 4.3 Stats - 统计模型

```python
@dataclass
class Stats:
    """统计数据模型"""

    total_nodes: int = 0
    valid_nodes: int = 0
    invalid_nodes: int = 0
    by_type: Dict[str, int] = field(default_factory=dict)
    by_country: Dict[str, int] = field(default_factory=dict)
    by_source: Dict[str, int] = field(default_factory=dict)
    avg_latency: int = 0
    updated_at: Optional[datetime] = None

    def to_dict(self) -> dict:
        """转换为字典"""
```

---

## 5. 工具模块接口

### 5.1 Logger - 日志工具

```python
def setup_logger(name: str, level: str = "INFO") -> logging.Logger:
    """
    配置日志器

    Args:
        name: 日志器名称
        level: 日志级别

    Returns:
        配置好的日志器
    """

def get_logger(name: str) -> logging.Logger:
    """
    获取日志器

    Args:
        name: 日志器名称

    Returns:
        日志器实例
    """
```

### 5.2 Network - 网络工具

```python
async def check_tcp_port(host: str, port: int, timeout: int = 3) -> bool:
    """
    检查 TCP 端口是否可达

    Args:
        host: 主机地址
        port: 端口号
        timeout: 超时时间

    Returns:
        是否可达
    """

async def http_request_via_proxy(
    url: str,
    proxy: dict,
    timeout: int = 10
) -> Tuple[bool, int]:
    """
    通过代理发送 HTTP 请求

    Args:
        url: 请求 URL
        proxy: 代理配置
        timeout: 超时时间

    Returns:
        (是否成功, 延迟毫秒)
    """
```

### 5.3 GeoIP - 地理识别

```python
class GeoIPLookup:
    """地理位置查询"""

    def __init__(self, db_path: str = "data/cache/GeoLite2-City.mmdb"):
        """
        初始化查询器

        Args:
            db_path: GeoIP 数据库路径
        """

    def lookup(self, ip: str) -> dict:
        """
        查询 IP 地理位置

        Args:
            ip: IP 地址

        Returns:
            {
                "country": "US",
                "country_name": "United States",
                "city": "New York",
                "isp": "Cloudflare"
            }
        """

    def close(self):
        """关闭数据库连接"""
```

---

## 6. 主入口接口

### 6.1 scripts/run.py

```python
async def main():
    """
    主函数 - 协调整个流程

    执行步骤:
    1. 加载配置
    2. 抓取订阅
    3. 解析节点
    4. 去重
    5. 测试节点
    6. 生成订阅
    7. 输出统计
    """

def run():
    """入口函数"""
    asyncio.run(main())

if __name__ == "__main__":
    run()
```

### 6.2 命令行参数

```bash
# 完整运行
python scripts/run.py

# 只抓取不测试
python scripts/run.py --no-test

# 只测试已有节点
python scripts/run.py --test-only

# 只生成订阅
python scripts/run.py --generate-only

# 指定配置文件
python scripts/run.py --config config/settings.yaml

# 调试模式
python scripts/run.py --debug
```

---

## 7. 错误处理

### 7.1 异常类定义

```python
class AuroraError(Exception):
    """Aurora 基础异常"""

class FetchError(AuroraError):
    """抓取异常"""

class ParseError(AuroraError):
    """解析异常"""

class TestError(AuroraError):
    """测试异常"""

class ConfigError(AuroraError):
    """配置异常"""
```

### 7.2 错误处理策略

```python
# 网络请求 - 重试
@retry(max_attempts=3, delay=2, backoff=2)
async def fetch_with_retry(url: str):
    pass

# 节点解析 - 跳过
for url in urls:
    try:
        node = parse_url(url)
    except ParseError:
        logger.warning(f"解析失败，跳过: {url}")
        continue

# 节点测试 - 标记失败
try:
    is_valid = await test_node(node)
except TestError:
    node.is_valid = False
```
